{
  "name": "aneeshkm-portfolio",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "description": "Personal portfolio for Aneesh Mokashi.",
  "scripts": {
    "generate": "node scripts/prepare-deploy.mjs && tsx src/render-static.jsx --out=dist",
    "dev": "node scripts/prepare-deploy.mjs && tsx src/render-static.jsx --out=dist && node scripts/serve-static.mjs dist",
    "build": "node scripts/prepare-deploy.mjs && tsx src/render-static.jsx --out=dist",
    "check": "node scripts/prepare-deploy.mjs && tsx src/render-static.jsx --out=dist && node scripts/check-static.mjs"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "tsx": "^4.22.3"
  }
}
